// import { AbstractControl } from "@angular/forms";

//     export function EmailPatternValidator(emailPattern:RegExp)
//     {
//         return (control:AbstractControl)=>{
//             const pattern=emailPattern.test(control.value);
//             return pattern ? {'forbiddenName':{value:control.value}} :null;
//         }
     
//     }
